#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char str1[] = "\"programming is like building a multilingual puzzle";

	puts(str1);
	return (0);
}
